<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nocode.example.mapper.slave.SlaveMemberMapper">
    <select id="findById" resultType="nocode.example.domain.Member" parameterType="int">
        <![CDATA[
            SELECT * FROM Member WHERE id=#{id}
        ]]>
    </select>
</mapper>